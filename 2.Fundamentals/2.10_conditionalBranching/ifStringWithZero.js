//Conditional branching

//if (a string with zero)

//Will alert be shown?

"use strict"

if ("0") {
    alert( 'Hello' );
  }

  // alert will be shown.
  // the condition of if is converted to a boolean
  // "0" is a string
  // a String containing any amount of any character returns true in boolean
  // so the code within the if exercutes
  // hence the alert appears.